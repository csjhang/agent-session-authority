import{describe,expect,it}from"vitest";import{collect_history,MockAcpMuxPeer,acp_mux_events_to_history}from"../src/index.js";describe("@asa/adapter-acp-mux fixture",()=>{it("models observation attach and reissue",()=>{const e=new MockAcpMuxPeer().run_fixture_scenario();expect(e.some(x=>x.type==="observer_attach")).toBe(true);expect(e.some(x=>x.type==="pending_permission_reissue")).toBe(true);expect(e.some(x=>x.type==="permission_resolved")).toBe(true)});it("converts history and notes non-claims",async()=>{const r=await collect_history({mode:"fixture"});expect(r.target).toBe("acp-mux");expect(r.history.some(x=>x.op==="approval.grant")).toBe(true);expect(r.notes.some(n=>/no fencing/i.test(n))).toBe(true)});it("LIVE fails closed",async()=>{await expect(collect_history({mode:"live"})).rejects.toThrow(/not implemented/)});it("digests deterministic",()=>{const a=acp_mux_events_to_history(new MockAcpMuxPeer().run_fixture_scenario()),b=acp_mux_events_to_history(new MockAcpMuxPeer().run_fixture_scenario());expect(a.find(x=>x.op==="action.bind")?.attrs?.action_digest).toBe(b.find(x=>x.op==="action.bind")?.attrs?.action_digest)})});
