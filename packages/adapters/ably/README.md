# @asa/adapter-ably

Ably AI Transport adapter. Prefer fixture mode (no key). Optional live path if ABLY_API_KEY is set.
Cloud-only: no self-host / air-gap. See targets/ably/authority-assessment.json.
