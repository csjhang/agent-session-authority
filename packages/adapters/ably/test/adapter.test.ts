import {describe,expect,it} from "vitest"; import {collect_history,MockAblyPeer,ably_events_to_history} from "../src/index.js";
describe("@asa/adapter-ably fixture",()=>{it("models HITL and first response wins",()=>{const e=new MockAblyPeer().run_fixture_scenario();expect(e.some(x=>x.type==="run_suspend")).toBe(true);expect(e.some(x=>x.type==="tool_approval_response"&&"ignored"in x&&x.ignored)).toBe(true);expect(e.some(x=>x.type==="run_resume")).toBe(true)});it("converts history",async()=>{const r=await collect_history({mode:"fixture"});expect(r.target).toBe("ably");expect(r.history.some(x=>x.op==="approval.grant")).toBe(true);expect(r.notes.some(x=>/cloud|air-gap/i.test(x))).toBe(true)});it("fails closed without key",async()=>{await expect(collect_history({mode:"live",env:{}})).rejects.toThrow(/ABLY_API_KEY/) });it("key still refuses unimplemented capture",async()=>{await expect(collect_history({mode:"live",env:{ABLY_API_KEY:"x:y"}})).rejects.toThrow(/not implemented/) });it("digests deterministic",()=>{const a=ably_events_to_history(new MockAblyPeer().run_fixture_scenario()),b=ably_events_to_history(new MockAblyPeer().run_fixture_scenario());expect(a.find(x=>x.op==="action.bind")?.attrs?.action_digest).toBe(b.find(x=>x.op==="action.bind")?.attrs?.action_digest)})});
