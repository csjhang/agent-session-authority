{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/csjhang/agent-session-authority/spec/authority-assessment.schema.json",
  "title": "AuthorityAssessment",
  "type": "object",
  "properties": {
    "target": { "type": "string", "description": "Assessment subject; copied into report.target." },
    "profile_version": { "type": "string", "description": "Profile version this assessment was run against." },
    "claims_file": {
      "type": ["string", "null"],
      "description": "Optional path to a claims document; null when using inline profile only."
    },
    "claim_sources": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Provenance URLs or citations for vendor / research claims."
    },
    "test_basis": {
      "type": "string",
      "enum": ["vendor_claim", "research_profile", "synthetic_fixture"],
      "description": "Evidence basis for findings (propagated to each CheckFinding)."
    },
    "notes": { "type": "string" }
  },
  "additionalProperties": true
}
