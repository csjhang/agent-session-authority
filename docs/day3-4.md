# Day 3-4 checklist

History JSONL schema + offline `asa check` path.

See `findings/2026-09-day3-4/README.md` for the completion record.

**Done:** parse/validate/serialize history; reject non-monotonic seq; CLI + report with distinct `result` / `claim_status`; Day 5-7 checkers already present for AUTH-01/03/05/06.
